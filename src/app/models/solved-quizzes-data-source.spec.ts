import { SolvedQuizzesDataSource } from './solved-quizzes-data-source';

describe('SolvedQuizzesDataSource', () => {
  it('should create an instance', () => {
    expect(new SolvedQuizzesDataSource()).toBeTruthy();
  });
});
